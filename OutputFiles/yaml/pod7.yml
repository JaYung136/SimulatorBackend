apiVersion: v1
kind: pod
metadata:
  name: HFSM1
  namespace: default
  labels:
    type: App
spec:
  nodeName: host8
  containers:
  - args:
    - HOSTNAME
    - KUBERNETES_PORT
    image: nginx
    name: containerHFSM1
    resources:
      requests:
        memory: 100Mi
        cpu: 1000m
    command:
    - printenv
