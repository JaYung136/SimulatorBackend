apiVersion: v1
kind: pod
metadata:
  name: HFSM2
  namespace: default
spec:
  nodeName: host1
  containers:
  - image: nginx
    name: containerHFSM2
    resources:
      requests:
        memory: 100Mi
        cpu: 1000m
