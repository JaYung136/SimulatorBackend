apiVersion: v1
kind: pod
metadata:
  name: HFSM3
  namespace: default
spec:
  nodeName: host2
  containers:
  - image: nginx
    name: containerHFSM3
    resources:
      requests:
        memory: 200Mi
        cpu: 1000m
